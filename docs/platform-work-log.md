# Cloudflare platform integration and validation

## Objective and boundaries

Integrate the multiplatform backend from thread `01a07a18-dac2-7571-993b-3c00d035afd7` into the Cloudflare migration, deploy and validate a dedicated environment, then implement backend user authentication and clip likes, comments, and favorites. OCR and all workflow execution remain in GitHub Actions. Existing production and the separate frontend repository must remain untouched.

Work branch: `codex/cloudflare-validation`, starting at migration commit `6a6d0ae`. Reviewed source platform commits: `c7c3863` and `ec4b870` on `dev`. The first integrated platform commit is `2923d2a`.

## Current state — 2026-09-08, after profile-claim deployment

- Validation runs `1fea429` with all seven database migrations applied. Auth/social routes, the multiplatform retry correction and the minute auth cleanup Cron are deployed; outbound integrations remain disabled and no login providers are configured.
- Hosted session/social mechanics passed with two short-lived synthetic identities, including CSRF/privacy/ownership, deletion/revocation, and public search/screenshot moderation. All fixture identities and owned rows were removed, and the real clip/image were restored unchanged. Real OAuth consent/browser behavior remains pending.
- Three complete source recordings passed: Bilibili `[0,2468)`, YouTube `0C6bxQsDj-s` `[0,2718)`, and YouTube `yS_mgLwtITA` `[0,2467)`. Together they processed 3,827 expected samples and published 39 detections/screenshots; the YouTube runs persisted 23 IGD readings. The Bilibili recording is a reviewed copy of the second YouTube recording, so these represent two distinct gameplay recordings across three source uploads. The earlier old-layout Bilibili interval adds 450 samples/four detections. Detailed evidence and accuracy limits appear below.
- Thirteen reviewed duplicate-fight groups are applied and both source-filtered public searches verified the corresponding 26 appearances. A fresh remote D1 audit at 18:44 UTC confirmed all four planned recordings/ranges completed, zero unfinished chunks, 43 clips/detections, 13 groups, no remaining user/session fixtures, and no foreign-key errors. Evidence: `.ignore/platform-validation/hosted-state-20260907.json`.
- Hosted automatic ingestion polled YouTube successfully and queued forty readiness candidates, but both Bilibili uploader feeds were rejected. All three leases finished; no OCR was dispatched. Independent local probes establish HTTP 412 and provider JSON −352 rejections. Automatic ingestion/provider access remains an open capability gap, independent of successful manual recording checks.
- Branch `3d00e92` includes the reviewed multiplatform retry correction with 173 passing local Worker tests and successful CI. After the user reauthenticated Wrangler, the independent D1 query passed and the exact tested snapshot deployed successfully. Hosted synthetic reprocessing passed on that build: exact anchors retain social records and moderation, adjacent anchors inherit none, and physical R2/SQL cleanup preserves all 43 original clips/detections. Detailed evidence is below and in [backend-validation-status.md](backend-validation-status.md).
- Hosted favorite/comment pagination and natural quota rollover also passed on `3d00e92`. Exact pages excluded hidden and other-user rows; five report requests succeeded, the sixth returned 429, and the next real D1 minute reset the same counter to one, then two. All fixtures were removed and all 43 original clip/detection row hashes remained unchanged.
- The reviewed branch-only workflow accepted real dispatch and ran successfully in the disposable GitHub Actions runner. The container exited and was removed after its one job. Ordinary validation CI passed for both `1c5428c` and `823082a`; unattended Worker deployment still requires the separate Cloudflare CI token.
- Full-schema snapshot tooling is committed and locally verified; creating a separate disposable database for remote import rehearsal awaits explicit approval after automatic approval review rejected that resource creation. No import-proof database was created.
- Existing dev/production and the separate frontend remain untouched. Remaining work includes automatic ingestion and dispatch/provider callbacks, a real Twitch recording on validation, real provider login/browser behavior, remote import rehearsal, and final acceptance. The [acceptance matrix](backend-validation-status.md) distinguishes established evidence from remaining work.

## Earlier checkpoint reported to the user — 2026-09-07, 18:00 UTC

This historical checkpoint records the state before the migration correction and subsequent hosted checks above. The migration goal remains active; this is not a production cutover or completion report.

- **Committed and pushed:** `e30795e` includes the Cloudflare/multiplatform backend, processing coverage checks, user authentication, persistent clips, likes, private favorites, comments, reports, moderation, and visibility propagation. [CI 34149072743](https://github.com/liftaris/bazaar-ghost/actions/runs/34149072743) passed. Its deployment steps were explicitly skipped because the separate validation Cloudflare CI token is absent.
- **Actually deployed:** validation health still reports `93c82c6`. D1 has migrations `0001`–`0003`. Applying `0004_auth.sql` failed remotely with `incomplete input: SQLITE_ERROR`; no auth/social tables were created and deployment of `e30795e` did not run. The previous Worker remains healthy. The validation auth signing secret was installed, but provider credentials, real OAuth consent, and hosted auth/social verification remain pending.
- **Full hosted recording verified:** [OCR 34148846218](https://github.com/liftaris/bazaar-ghost/actions/runs/34148846218), running processing code `55a9deb`, completed Bilibili part `BV1K114BiE7t:33726794861` across its full cataloged `[0,2468)` timeline. Two chunks processed 900 + 334 = **1,234/1,234 expected samples**. The terminal VOD/chunk states, **13 detections**, public search results, and all **13 decodable JPEGs** match the Actions artifacts. Evidence: `.ignore/platform-validation/full-bilibili-evidence.json`. This proves sampled coverage and persistence, not exhaustive matchup recall or correctness of every OCR name.
- **Earlier hosted recording interval verified:** `[600,1500)` of old-layout part `BV1FfL5zPEbH:29594289602`, 450/450 samples and four retrievable detections/screenshots, as recorded below. This is an interval within a longer upload.
- **IGD on real footage:** the actual local SFDE pipeline processed 198/198 samples across eleven reviewed matchup windows from old/current Bilibili and YouTube. Ten accepted days were visually correct; one native-360p day-4 reading remained null below the existing confidence threshold. These used a recording-only backend. Both hosted Bilibili runs had IGD disabled, so hosted real-day persistence is still unverified. Evidence: `.ignore/igd-validation/README.md`.
- **Prepared locally, not yet committed or exercised remotely:** the isolated one-job GitHub Actions runner/workflow and source-schema-3 to target-schema-6 snapshot tooling. Their helper/migration tests and the populated real local D1 import proof passed. YouTube still requests human sign-in from GitHub-hosted runners; the same public video resolves anonymously from the local container. The temporary runner has not yet been registered or dispatched.

Existing dev/production resources and the separate frontend remain untouched. Validation has outbound integrations disabled and sends no Discord notifications. Its currently deployed Worker has no active Cron schedules. The auth maintenance Cron exists only in the newer, undeployed configuration. Automatic ingestion/Worker-to-GitHub dispatch and live provider callbacks still need hosted acceptance evidence.

## Execution plan

- [x] Review source multiplatform changes, record defects and port contracts.
- [x] Integrate platform identity, catalog/discovery/archive ingestion, media resolution, verified appearances, webhook handling, and search into Workers/D1/GitHub workflows. Initial integration committed as `2923d2a`; review fixes continue locally.
- [ ] Validate the integrated pipeline locally with platform-specific tests and real OCR processing.
- [x] Provision separate Cloudflare resources and GitHub environment, with branch-bound deployment and processing workflows.
- [ ] Deploy only the validation environment and run hosted API, queue, database, storage, and GitHub processing checks; correct and redeploy failures.
- [x] Design and implement user authentication, sessions, account lifecycle, and clip identity. Committed and tested; hosted acceptance remains below.
- [x] Implement likes, comments, favorites, ownership, moderation, and abuse controls, with public/private API contracts. Committed and tested; hosted acceptance remains below.
- [ ] Adversarially review implementation and tests; address findings and validate hosted functionality.
- [ ] Publish operational documentation, API examples, deployment/test evidence, and a requirement-by-requirement completion audit.

## Verification requirements

Completion requires authoritative evidence for every plan item, including actual isolated hosted deployment and GitHub jobs. Local mocks do not prove provider permissions or live media access. Authentication must verify identity through a real supported flow; test identities must never become a deployed authentication bypass. Likes and favorites must be idempotent and distinct; comments must enforce author/moderator authorization. No browser client receives admin or processor credentials.

Production safeguards: no pushes to `main`/`dev`, no production/development resource mutations, no changes to existing provider callbacks, no real notification delivery from validation, and no broad repository rule changes when branch/environment-scoped controls suffice. New resources must have distinct names and identifiers. Keep test evidence free of credentials and personal data.

## Activity

### 2026-09-08 local / 2026-09-09 UTC — ingestion/profile revision deployed and checked

- Committed and pushed `1fea429` only to the validation branch. All 1,205 application/config/test/template file hashes match the frozen local snapshot, which passed 211 Worker tests, 128 Python tests and 129 OCR tests. [CI 34296335605](https://github.com/liftaris/bazaar-ghost/actions/runs/34296335605) passed; its migration/deploy/health steps were skipped because the dedicated CI credential is still unavailable.
- The first migration attempt returned Cloudflare 7403. After the user restored Wrangler login, the same migration command applied only `0007_platform_ingestion_dispatch.sql` successfully (two commands, 1.20 ms). Deployed the exact archived commit through local Wrangler as version `20fa6904-f2c2-4f2b-8ca6-5ea78a6685b7`: upload 2,507.90 KiB / gzip 467.42 KiB, startup 61 ms. Exact public health, empty login-provider list and all 43 public clips were verified. Outbound remains false and the minute auth Cron remains the only trigger; no original dev/production resources changed.
- Hosted profile-claim proof passed from `2026-09-09T00:51:41.044Z` to `2026-09-09T00:51:54.696Z`. Required expectations, stale crop/era/ID rejection, normalized JSON values, active crop/streamer conflicts and edit-after-release all matched the tested contract. All exact fixture rows were removed, foreign keys were clean and original 43 clip/detection full-row hashes were unchanged. No new screenshots, users or provider calls. Root reviewed the final harness after the independent reviewer caught a failed-state assertion and unbounded child settlement; both were fixed and local FD/deadline checks passed before execution.
- Evidence: `.ignore/platform-validation/deployment-1fea429.json`, `ingestion-profile-integrated-tests.json`, `profile-claim-root-review.json`, and `profile-claim.a8c39d91-e434-4b12-87d7-b35e81950d73/proof.json` under the same directory. Recurring ingestion code is deployed but provider activation and real automatic completion remain pending. Real Twitch full-recording validation is the next media acceptance step.
- The user confirmed Better Auth as the intended implementation choice. Better Auth owns OAuth/session primitives; the Worker owns the application integration and associated account/security rules. This remains a maintained library deployed with the application, with real provider consent still pending.


### 2026-09-07 local / 2026-09-08 UTC — recurring ingestion and profile consistency

- Found that `ingest-platforms.yml` had manual/reusable entry points but no recurring caller. The existing provider-processing Cron can now queue a durable dispatch ticket when requested discovery/candidates or enabled-account work is due. Database-clock leases, exact GitHub run/attempt ownership, ambiguous-response backoff and idempotent finish retries prevent ordinary redelivery from creating competing catalog runners. It does not initialize new discovery merely by being enabled. Validation still has only the auth Cron and outbound remains disabled; actual automatic GitHub/provider acceptance is pending.
- Migration `0007_platform_ingestion_dispatch.sql` adds only deployment-local operational state. The converter now requires empty target schema 7 while preserving source schema 3; exported dispatch tickets are rejected, and occupied target tickets cannot be overwritten. All 20 migration tests and a real local D1 import proof passed, including four derived clips and zero foreign-key violations. No remote import resource was created or used.
- Extended the disposable Actions validator to existing cataloged Twitch VODs, up to six hours and twelve chunks of at most 1,800 seconds. It preserves YouTube cataloging, checks exact source/chapter/profile/template/rendition contracts, and paginates public results before comparing the complete set. All 19 validator tests passed with the existing system Pillow installation, including a mocked 17,085-second ten-chunk run and real JPEG validation. These are harness tests, not a completed Twitch recording.
- Reviewed public Twitch metadata identifies VOD `2863728070` as `nl_Kripp` (owner `29795919`), duration 17,085 seconds, with one full Bazaar chapter. Ordinary anonymous Streamlink/FFmpeg access decoded frames at 600, 7,200 and 16,500 seconds. The day clock is readable and clear of the camera overlay in those samples; sparse shop frames do not certify the matchup banner crop or new hosted IGD values. No catalog rows or OCR results were created by these checks.
- Review exposed shared-profile and Twitch-streamer edits bypassing the existing platform-account guards. Active-work edits now use a D1 transaction, while reservations compare consumed crop/edge values as well as profile ID. Numeric JSON comparison avoids rejecting `0` versus `0.0`, and metadata-only changes remain allowed. The processor sends its actual parsed profile/template selection at claim time; the HTTP claim requires them and rejects stale preparation before taking ownership. Older processor images must be rebuilt because they omit the required fields. Settings are not retroactively applied to completed chunks.
- Final combined verification passed **211 Worker tests across 19 files, 128 Python tests with one opt-in native-D1 proof skip, and 129 offline OCR tests**. TypeScript, branch/resource isolation and the validation bundle dry-run passed; upload is 2,507.90 KiB / gzip 467.42 KiB. All application file hashes match the frozen isolated snapshot. The separate schema-7 native D1 proof already passed. Initial isolated test setup omitted dummy bindings and the OCR metrics plugin; both setup mistakes were corrected and the affected suites rerun successfully. No application test failure remains. Evidence: `.ignore/platform-validation/ingestion-profile-integrated-tests.json`. This batch remains local until an actual deployment entry identifies its live revision.


### 2026-09-07 local / 2026-09-08 UTC — approved deployment and actual minute cleanup

- The user explicitly approved the prepared validation push/deploy/test action. Pushed `fe0bd1c` only to `codex/cloudflare-validation`, then deployed its exact archived snapshot using local Wrangler. All ten changed application/config/test files matched the 182-test local snapshot. Worker version is `6b52c8a7-2492-4d07-99fb-9db073050cf4`; upload 2,497.97 KiB / gzip 464.89 KiB; startup 93 ms. Health reports the exact build and validation environment; all 43 real clips remain available. No new resources or original dev/production changes.
- [CI 34177261269](https://github.com/liftaris/bazaar-ghost/actions/runs/34177261269) passed. Its actual deployment steps were skipped; authenticated local Wrangler supplied the authoritative deployment. The only trigger is `* * * * *`, with outbound integrations still disabled.
- A reviewed read-only tail confirmed actual minute activation at `01:46:08Z`, with a successful outcome, no exceptions, and valid aggregate cleanup metrics. The event's eight-second offset also demonstrates why top-of-minute equality is too strict for observation. It does not retroactively certify the earlier hourly event, whose raw metadata was not retained.
- The independently reviewed two-counter harness executed once and exited successfully at `01:50:12.075Z`. Both full counter rows were present before the prior minute and remained live at its actual `01:49:08Z` event. At `01:50:08Z`, one had naturally expired: the Worker deleted it, reported one direct deletion / 18 rows read / one row written, and preserved the future control unchanged. Exact finally-cleanup verified zero owned rows and clean foreign keys; the tail exited zero. No users, sessions, media, R2 objects or provider calls were part of this probe.
- Evidence: `.ignore/platform-validation/deployment-fe0bd1c.json`, `auth-activation.1013937f-ad97-48c0-8c2a-236dbd0be5a4/proof.json`, and `auth-minute.a7bd4e85-9d72-4e5f-8f4d-5580f9a54283/proof.json` (the latter two under `.ignore/platform-validation/`). The earlier push gate is resolved; real provider OAuth and unattended integration credentials remain separate dependencies.
- A bounded anonymous metadata read of Twitch VOD `2863728070` confirmed `nl_Kripp`, duration 17,085 seconds, one full-duration The Bazaar chapter, and eight available formats. No cookies, media download, catalog insertion or OCR occurred. The current disposable validator's one-hour cap and YouTube-only enrollment need a reviewed extension or a shorter complete candidate. Evidence: `.ignore/platform-validation/twitch-public-metadata-2863728070.json`.

### 2026-09-07 local / 2026-09-08 UTC — validation push approval gate

- The reviewed authentication maintenance change is committed locally as `961f56f`; all ten changed source/config/test files match the isolated snapshot that passed 182 Worker tests. TypeScript and validation resource/branch isolation were rechecked after applying it.
- Automatic approval review rejected the validation-branch push twice, including after the active goal's dedicated-environment instructions and the exact branch/repository/diff were supplied. The stated reason was that earlier `dev` push permission did not authorize this distinct validation branch. No rejected action was bypassed; the remote branch remains `b0656e4` and hosted build remains `3d00e92`.
- The concrete next action is to push the reviewed validation commits, deploy the exact application snapshot only to existing Worker `bazaarghost-validation`, and run the prepared two-counter minute-Cron proof after propagation. Explicit user confirmation for that action is pending. No new resources, original dev/production deployment, or real notifications are part of it.

### 2026-09-07 local / 2026-09-08 UTC — auth maintenance capacity and scheduled acceptance preparation

- Previous-turn work completed hosted retry, pagination, and natural quota rollover; this turn continues with actual session expiry and scheduled cleanup. The live Worker remains `3d00e92` with the hourly `17 * * * *` schedule. No cadence change has been deployed yet.
- Independent review found safe cleanup predicates but a practical throughput limit: each hourly run deletes at most 500 expired rows per private table and 100 aged abandoned registrations. Nine addresses reading one rate-limited auth endpoint each minute can create 540 rate keys per hour, already exceeding that cleanup capacity. Authorization expiry still applies immediately; physical retention is separate.
- Two new local D1 tests verify 501 expired rows per table and 101 abandoned registrations: each pass removes only its oldest bounded batch, a second drains the remainder, and live/fresh/previously registered accountless controls survive. Both passed in workerd.
- A reviewed change is prepared in an isolated local checkout, retaining the same per-run bounds but running auth maintenance every minute. It adds aggregate direct deletion counts, actual D1 rows read/written, and elapsed time without private identifiers. `DELETE ... RETURNING 1` distinguishes direct deletions from cascade work. All **182 Worker tests across 16 files**, 12 deployment/configuration tests, strict TypeScript, and the validation bundle dry-run passed. The main worktree's hourly config and hosted build remain unchanged during the existing-schedule proof.
- The independently reviewed Cron observer is actually connected to the exact validation Worker version, verified by matching public health events. It retains only allowlisted scheduled metadata; request headers, arbitrary log bodies, and CLI diagnostics are discarded. The first observer exited before connecting because Wrangler rejects an explicit sampling rate of 1; removing that invalid filter fixed it. The failed attempt is preserved separately and cannot satisfy the acceptance check.
- The reviewed fixture executed once from `01:10:00.095Z` to `01:20:55.894Z`. Natural session expiry passed: reads and previously valid CSRF writes changed from 200 to 401 without changing the mutation counter. Five untouched expired witnesses were present at `01:11:06.306Z` and absent by `01:17:50.981Z`; all live/fresh/previously registered control row hashes stayed identical and live requests still succeeded. The overall scheduled-event check exited inconclusive because the exact observer event was absent; no retroactive change to its acceptance criteria was made. Independent review recomputed the saved control/original row hashes and verified all twelve zero fixture/FK cleanup categories. All fixtures were removed, foreign keys were clean, and all 43 original clip/detection full-row hashes remained unchanged.
- The observer stayed connected and saw 44 health events, but its exact top-of-minute predicate captured no scheduled event. Primary Cloudflare repository examples contain scheduled timestamps with seconds inside the minute ([example](https://github.com/cloudflare/mcp-server-cloudflare/issues/418)); this is a likely observer defect, not a proven explanation for this specific discarded event. The next narrowly scoped probe will retain actual numeric timestamps within the selected UTC minute and aggregate cleanup metrics. No raw event was retained, so the old run cannot be retroactively certified.
- After fixture cleanup, the reviewed minute-cadence patch was applied to this branch. The same five deletion predicates and per-invocation limits remain; only cadence and aggregate observability change. Other provider schedules are preserved and only validation will be deployed. Hosted minute-cadence proof remains pending. Evidence: `.ignore/platform-validation/hosted-auth-expiry.ad0dddb6-7f66-4f71-b488-55811b8384d6/proof.json` and `.ignore/platform-validation/auth-cron-20260908T0117.json`.

### 2026-09-07 local / 2026-09-08 UTC — additional source evidence

- A local visual audit directly certified six of the 23 stored YouTube day readings; seventeen remain unresolved from the available images. Two unresolved values have readable corroboration from the reviewed Bilibili copy, explicitly separate from direct YouTube evidence. No disagreement was found among the six directly inspected values. Evidence: `.ignore/platform-validation/igd-23-local-visual-audit.{md,json}`.
- The earlier static Twitch page read was inconclusive. A fresh anonymous Chromium read with a bounded JavaScript-rendering wait subsequently exposed 30 real VOD listings. The shortest listed candidates were 3:55:57, 4:44:45, and 5:07:20; no short candidate, playback access, complete Bazaar timeline, or processing profile has been verified yet. No Twitch recording was downloaded, cataloged, or processed. Evidence: `.ignore/platform-validation/twitch-public-directory/proof.json`.

### 2026-09-07 local / 2026-09-08 UTC — hosted pagination and natural quota rollover

- A separately reviewed synthetic fixture ran once on `3d00e92` from `00:27:43.647Z` to `00:29:07.723Z` and exited successfully. It created two short-lived users, one disabled/unready YouTube-shaped account/recording, and five durable clips without detections, chunks, ingestion jobs, or R2 objects. No application source change was needed.
- Favorites returned Alice's exact pages `[49,51]`, then `[52]`, skipping hidden clip 50; the cursor was 51 followed by null. Bob saw only his clip 53. Comment pages returned the first/third entries, then the fifth, excluding a hidden middle row and suspended author. The first comment cursor matched database sequence 11, followed by null.
- Exactly eight retries of one report produced `200,200,200,200,200,429,200,200`. The initial six requests were verified in D1 minute `29813788`, with one report/counter and count five. After waiting for actual minute `29813789`, the next two requests reset that counter to one and incremented it to two. No counter or clock was edited; this proves the shared quota path through reports, alongside the earlier hosted reaction-limit proof.
- Cleanup verified fifteen zero fixture/FK counts and unchanged full-row hashes for all 43 original clips and detections. An independent reviewer parsed the saved D1 results, recomputed those row hashes, and verified the actual clock/counter progression. Three genuine clip-hide/comment-hide/user-suspension audit records remain. No R2 objects were created. Seed SQL and anonymous capture backing paths were removed; retained logs have restrictive permissions. Evidence: `.ignore/platform-validation/hosted-social-pagination.7e89cbab-8d7e-42ec-a081-0d6a63505b73/{proof.json,independent-evidence-review.json}`.
- A bounded public Twitch directory/search read did not establish a verifiable recording ID, duration, or playable Bazaar timeline. No Twitch candidate was nominated or processed; this is a public-page extraction limitation, not evidence of a provider access rejection. The remaining real Twitch validation dependency is unchanged.

### 2026-09-07 local / 2026-09-08 UTC — hosted reprocessing and independent cleanup audit

- The reviewed fixture harness ran once against exact validation build `3d00e92`, from `00:14:32.849Z` to `00:15:19.811Z`, and exited successfully. Two explicitly synthetic short-lived signed sessions and one disposable 60-second YouTube-shaped recording exercised the real processor/admin APIs; there was no provider OAuth or media OCR in this check.
- Actual claim, upload, publish, completion, unified admin retry, clear, and republication preserved clip 44 at timestamp 12 with its like, private favorite, and comment. Publishing adjacent timestamp 13 first inherited none of these records. A stale claim was rejected. Hidden status at 12 survived another complete retry and publication cycle.
- Warmed public view and both search RPCs excluded the hidden fixture; both screenshot aliases and GET/HEAD/ETag requests denied it. The hide/recheck sequence took 1,338 ms, less than the 30-second internal cache lifetime. Physical R2 reads independently verified published image hashes and clear/replacement behavior.
- Finally-cleanup verified all five uploaded JPEG keys absent, plus the never-created access probe (six tracked keys total). Every fixture-owned database count and foreign-key violation count was zero. All 43 pre-existing clip rows and 43 detection rows retained their full-row hashes. Seed SQL and anonymous capture files were removed; retained logs have restrictive permissions and contain no tracked keys or claim tokens.
- Independent read-only D1 verification at `00:17:31Z` confirmed four real videos, 43 clips, 43 detections, zero users/accounts/sessions/social records, no fixture video/clips, and clean foreign keys. One genuine fixture hide audit (`75c436b5-e840-4d8b-b206-c519865705c8`) remains intentionally. Evidence: `.ignore/platform-validation/hosted-reprocess-smoke.f5cd8059-2c6d-434f-b379-5ca162e4556f/{proof.json,independent-cleanup-proof.json}`. Independent source/evidence review approved the assertion and cleanup scope.
- A separate local audit found no source defect in the remaining pagination/next-window quota checks: filters precede limits, cursors use immutable ordering, and the shared quota update resets inside the action transaction. A bounded hosted proof is being prepared; no hosted success is claimed yet.

### 2026-09-07 — retry eligibility defect found during reprocessing preparation

- Inspection of the hosted reprocessing path found `retryVod` still joining `vods` to Twitch `streamers`. Independent YouTube/Bilibili recordings therefore failed eligibility; linked recordings incorrectly inherited the Twitch streamer's enablement. A local SQLite reproduction using all six migrations returned no row for an independent YouTube recording and the disabled Twitch flag for a linked recording, while `vod_processing_context` correctly returned the enabled platform account in both cases.
- The local fix uses the shared processing context and checks current availability/readiness/enablement and absence of active workers in the same D1 transaction as the chunk reset. Default retry still preserves completed siblings; `all` remains explicit. Concurrent eligibility changes or new claims return a conflict without resetting siblings.
- Added eight Worker regression cases across all three sources, account-versus-Twitch enablement, and real database writes injected between preflight and reset. The first Worker test process could not start because the sandbox denied its 127.0.0.1 listening socket. After local runtime approval became available again, all eight cases passed in workerd, followed by the full **173-test suite across 14 files**. Strict TypeScript, deployment-isolation and whitespace checks pass. Independent local review approved the shared eligibility and transactional guards without findings.
- A separate offline diagnostic bundled the actual updated `retryVod` implementation and executed it against Node SQLite with all six migrations and a small D1-shaped adapter. Eight cases passed: three source retry/preservation paths, two account-enablements, and three competing writes. Evidence: `.ignore/platform-validation/retry-sqlite-proof.json`; helper: `retry-sqlite-proof.mjs`. This proves the function's SQLite behavior, not Cloudflare binding/remote-parser behavior or hosted reprocessing with social records.
- Committed and pushed the correction as `3d00e92bbf0e5f7058eef8e5627ce67303947765`. The exact archived snapshot at `/tmp/bazaarghost-validation-3d00e92` passed a validation-config deployment dry-run (2,497.46 KiB, 464.73 KiB gzip). [CI 34171462120](https://github.com/liftaris/bazaar-ghost/actions/runs/34171462120) is the existing verification run to follow. Existing validation build `1c5428c` still has the retry defect; no real recordings were retried during this diagnosis. Hosted reprocessing with social records remains pending the corrected deployment.
- That CI run completed successfully; its actual deployment steps were skipped because the dedicated validation CI credentials remain absent. After the user's Wrangler reauthentication, deployed the exact `3d00e92` snapshot through local CLI to Worker version `cf381092-3bf5-4249-8ec9-7db69143d99f`. Startup was 57 ms. Public health confirms the full SHA, the auth-provider endpoint returns enabled with no providers, and all 43 public clips remain. Auth cleanup remains the only Cron and outbound calls remain disabled. Evidence: `.ignore/platform-validation/deployment-3d00e92.json`. This supersedes the earlier undeployed status; hosted reprocessing remains separate.

### 2026-09-07 — automatic ingestion and safe provider diagnostics

- [Run 34152338750](https://github.com/liftaris/bazaar-ghost/actions/runs/34152338750) used the committed bounded workflow controls (`limit=3`, `seconds=90`, discovery/dispatch disabled). Both Bilibili account jobs finished into retryable waiting states with errors. The YouTube account poll finished without an error and persisted forty candidate readiness jobs. The aggregate run failed for the two feed errors, with zero OCR dispatches. No cursor/backoff was reset to force another provider request.
- Independent probes with the exact application feed URLs and installed yt-dlp 2026.08.19 reproduced Bilibili HTTP 412 and JSON −352 locally. The URLs are supported by the extractor; public feed access, rather than a demonstrated URL-adapter bug, prevents automatic polling. Known individual recordings remain accessible. Evidence: `.ignore/feed-evidence/README.md`, `feed-probe.json`, and `.ignore/platform-validation/ingestion-34152338750/platform-ingestion/ingestion-result.jsonl`.
- Added finite typed diagnostics at metadata/feed extraction boundaries and propagated them through durable ingestion errors and JSON summaries. Arbitrary response bodies, subprocess stderr, URLs, and exception messages cannot become diagnostic output. Failed scans preserve their cursors/backoff and healthy later work continues. The local script suite ran 99 tests: 97 passed and two were explicitly skipped (optional local D1 import proof and unavailable host Pillow). All six diagnostic tests also passed with `ResourceWarning` treated as an error. Remote validation of this diagnostic revision is still pending.
- Committed and pushed diagnostics as `537e2a94c1ac8e08229479fe140368f78821aa4e`. Independent review classified the actual captured Bilibili stderr correctly and passed 37 targeted tests. Dispatched [ingestion run 34153730082](https://github.com/liftaris/bazaar-ghost/actions/runs/34153730082) with two jobs, a ninety-second start budget, and discovery/OCR dispatch disabled. Dispatch returned that run URL; its outcome has not yet been observed. Automatic approval review rejected the subsequent GitHub status check because the workspace was out of credits, and both active review agents stopped for the same reason. Resume by inspecting this exact run and the CI run for `537e2a9`; do not start replacement work merely because observation was interrupted. The live Worker remains `1c5428c`; this patch changes Python runners and requires no Worker redeployment.
- After approval tools became available again, inspected that same run rather than restarting it. It completed with the expected failure on two YouTube metadata jobs: each artifact row reports `auth_required`, retryable `waiting`, and `lease_finished=true`; the summary has two jobs/two errors/zero cataloged/zero dispatched. [Diagnostics CI 34153688580](https://github.com/liftaris/bazaar-ghost/actions/runs/34153688580) succeeded. Artifact: `.ignore/platform-validation/ingestion-34153730082/ingestion-result.jsonl`. A separate current-state D1 query failed and is being diagnosed; artifact lease acknowledgements are not presented as an independently reread database result.
- The failed management read returned Cloudflare error **7403**, “The given account is not valid or is not authorized to access this service,” for the exact validation D1 query. No database mutation was attempted. Automatic approval review separately rejected a local credential-variable-presence probe as credential discovery and rejected the review agent's names-only Worker secret inventory; neither probe ran and no alternate credential-discovery route was attempted. The user has been asked to restore Wrangler access to the existing validation account. Until then, do not claim an independent D1 reread or deployment of the retry fix. The prepared read-only verifier remains `.ignore/platform-validation/verify_ingestion_34153730082.py`.
- The user subsequently reauthenticated Wrangler, and the same prepared query succeeded. Both exact video jobs remain waiting with `auth_required`, attempt count 1, and null lease token/expiry. D1 contains 43 clips and 43 detections, no active ingestion jobs, and no foreign-key errors. This completes the independent durable-state check for the diagnostics run; it does not remove YouTube's runner-access restriction. Evidence: `.ignore/platform-validation/ingestion-34153730082/proof.json`. No denied credential-discovery probe was repeated.

### 2026-09-07 — three full source recordings and reviewed duplicate groups

- [YouTube run 34150684415](https://github.com/liftaris/bazaar-ghost/actions/runs/34150684415) completed all 2,718 seconds of `0C6bxQsDj-s` at processor commit `823082a`: 900 + 459 = **1,359 samples**, 13 detections and fully decoded screenshots, and 11 persisted IGD readings. Initial/final backend health identify the same deployed `1c5428c`. An independent local artifact/visual audit confirms days 1/2/3 at 218/422/618 and the intentional null at 788 despite a visible day 4. Other accepted days are not thereby visually certified. The reviewer could not repeat fresh network checks under its tool permissions; HTTP/D1/JPEG checks for this run are the executed Actions verifier's evidence, with that provenance explicit in `.ignore/platform-validation/youtube-34150684415-independent.md`.
- [YouTube run 34151218515](https://github.com/liftaris/bazaar-ghost/actions/runs/34151218515) completed all **2,467 seconds** of `yS_mgLwtITA` at processor commit `c43bcd1`: 900 + 334 = **1,234 samples**, including the final tick at 2466, 13 detections/decoded screenshots, and 12 persisted IGD readings. This exercises an odd-length EOF through the actual hosted persistence path. Its Bilibili copy advertises 2,468 seconds; their distinct durations are preserved.
- Both temporary Actions runners completed one job, removed their local credentials, exited, and removed their containers. Subsequent exact-name GitHub runner queries returned empty lists. The observed branch-only dispatches required no default-branch change.
- A reviewer compared all thirteen matchup frame pairs and additional battle boards, and checked Bilibili's explicit link to the YouTube original. Root also inspected a comparison sheet. The prepared plan was applied as **thirteen separate groups**, each joining exactly one fight's two detection IDs. Both source-filtered public group searches returned exactly those thirteen groups and 26 appearances. Repeated Raff, SpaceBunny and Layzyn fights remain separate. Actual uploader identities and raw detections are preserved. Evidence: `.ignore/platform-validation/duplicate-pair-review.md`, `duplicate-group-plan.json`, and `duplicate-group-evidence.json`.
- The three full uploads comprise two distinct gameplay recordings because one is a reviewed duplicate. Including the earlier old-layout interval, validation retains **43 real detections/clips**. Successful sampling/persistence and selected visual checks do not establish exhaustive recall or perfect OCR accuracy.

### 2026-09-07 — additional hosted social concurrency and lifecycle acceptance

- The second fixture-only HTTP/D1 proof passed on unchanged `1c5428c`: racing same-version edits produced one success/one conflict; repeated older moderation decisions did not undo newer decisions; concurrent report resolution produced one decision; and a later dismissal survived retrying the original resolution.
- Exactly 121 real like requests, with at most eight in flight, produced **120 successes and one HTTP 429** in one database minute. The persisted counter was 120 and there was one like. No counters were preseeded to manufacture the boundary.
- Suspension immediately revoked the session and hid the user's comment/like. Reactivation restored contributions but did not reactivate the revoked cookie. Deleting the other fixture account preserved the first user's comment and the shared real clip.
- Cleanup verified every fixture-owned user/account/session/reaction/comment/report/rate row absent and all 43 original real clip/detection IDs preserved. Real clip 1 was never moderated in this second proof. Six fixture moderation audit decisions remain as an honest history. Evidence: `.ignore/platform-validation/hosted-social-expanded.b6e1dabe-9a75-4ecc-8f58-3841f0355cbf/proof.json`. Real provider OAuth/browser consent remains untested.

### 2026-09-07 — authentication and social backend deployed

- Committed/pushed `1c5428c` and successfully applied remote migrations `0004`–`0006` from that exact snapshot. Deployed Worker version `55249edb-32ec-4b74-9ba8-09367b57a627`; public health confirms the exact build, `/api/auth/providers` returns enabled with no configured providers, and `/api/v1/clips` serves the real Bilibili detection backfill (17 clips).
- The hourly auth cleanup Cron is now deployed. Outbound integrations remain disabled. This supersedes the earlier checkpoint's migration/deployment blocker; real provider OAuth and hosted social acceptance remain separate checks.
- Added separate validation profile 2 with the visually reviewed IGD crop `[0.1458,0.4509,0.013,0.0231]` for subsequent recording checks; existing profile 1 remains unchanged.
- Reviewed temporary Actions runner verifies exact source/chunk identity, full sampled coverage including a 2,718-second two-chunk recording, exact public detection counts, and bounded full JPEG decoding. Nine helper tests pass in the offline OCR image. The image has no host mounts or Docker socket and accepts one explicitly dispatched validation job. Actual registration/dispatch remains pending.
- Updated snapshot imports to require target schema 6 while preserving the source schema 3 contract. Independent review passed 17 migration tests and the populated real local D1 proof, including all target table counts, derived clips, preserved triggers and unsafe-import rejection. The proposed separate remote import-proof database was not created: automatic approval review rejected creation pending explicit permission for that temporary resource; user approval has been requested.

### 2026-09-07 — remote D1 migration parser correction

- A disposable table in the isolated validation database reproduced the failure: a trigger containing bare `SELECT CASE ... END` fails through the remote D1 query endpoint with `incomplete input`, while the equivalent trigger-level `WHEN` guard succeeds. The probe table was removed after each attempt. Evidence: `.ignore/platform-validation/d1-parser-probe.json`.
- Changed the three not-yet-applied authentication guard triggers to `WHEN` predicates with the same abort conditions. The full local Worker suite still passes: **165 tests across 13 files**. Existing applied migrations `0001`–`0003` are unchanged.
- The remote behavior matches the failure described in [Cloudflare workers-sdk issue 4727](https://github.com/cloudflare/workers-sdk/issues/4727). The actual remote reproduction, rather than the older issue's status or local parser success, determines this correction.

### 2026-09-07 — initial inspection

- Confirmed clean migration worktree at `6a6d0ae`; main checkout is `dev` at `ec4b870`.
- Created dedicated local validation branch without changing the original checkout.
- Started independent source review, environment isolation review, and auth design/adversarial review agents.
- At initial inspection the Cloudflare migration had local/development/production templates only; the isolated environment was subsequently provisioned and deployed as recorded below.

## Decisions and evidence

The activity entries below distinguish the committed/deployed baseline, local follow-up work, and unfinished requirements. Test and deployment success alone do not establish end-to-end provider processing.

### 2026-09-07 — integration and isolation

- Ported independent YouTube/Bilibili creator identities, stable BV/CID parts, source-aware processing/profile/template context, separate public platform search, and reviewed appearance groups into D1/Workers.
- Kept public Twitch views scoped to Twitch, and fenced Twitch availability/chat jobs so they cannot misclassify other platforms.
- Ported catalog/discovery/archive runners and Streamlink/yt-dlp media adapters to the explicit catalog/processor HTTP APIs; OCR stays in GitHub Docker jobs.
- Replaced PostgreSQL locks with transactional D1 assertions. Independent tests reproduced concurrent timeline edits, account disables, and expired jobs; planning now checks the current timeline and active lease within its write transaction.
- Added namespace-aware signed YouTube WebSub handling, atomic receipt/job persistence, per-account lifetime replay digests, and optional hub renewal in the Worker.
- Found and fixed an additive schema watermark issue: the VOD table rebuild must preserve deleted-ID AUTOINCREMENT history as well as current rows.
- Provisioned only new validation resources: Worker name `bazaarghost-validation`; D1 `4159adb1-b0af-4a3e-9a0c-755dfe5d822c`; R2 `bazaarghost-validation-detections` and `bazaarghost-validation-logs`; Queues `bazaarghost-validation-jobs` and `bazaarghost-validation-dlq`.
- Created GitHub environment `validation`, restricted to `codex/cloudflare-validation`; configured its independent processor/catalog credentials and backend URL. Existing dev/production environments, callbacks, and resources were not changed.
- Validation endpoint: `https://bazaarghost-validation.kaio-8df.workers.dev`. The initial deployment is recorded below.
- A dedicated branch pipeline tests and optionally deploys the exact commit. A durable validation-scoped Cloudflare API token for unattended GitHub deployment is not yet configured; the authenticated local Wrangler CLI can deploy the validation environment meanwhile.

Review evidence: `.ignore/multiplatform-review.md`, `.ignore/environment-review.md`, `.ignore/platform-upgrade-review/evidence.json`, `.ignore/auth-design.md`. Resource evidence: `.ignore/platform-validation/provisioning.json`. Local evidence is distinct from the hosted observations below.

### 2026-09-07 — first isolated deployment and runner checks

- Committed and pushed `2923d2a` to `codex/cloudflare-validation`, then deployed that commit to the validation Worker. Public `/health` returned HTTP 200 with `environment=validation` and `build_commit=2923d2a…`.
- [Validation CI run 34145737889](https://github.com/liftaris/bazaar-ghost/actions/runs/34145737889) passed **98 SFDE/OCR tests, 78 Worker tests, and 61 script tests** on `2923d2a`. Its deployment job completed but skipped actual deployment steps because the dedicated Cloudflare CI token is not configured. The first Worker deployment instead used the authenticated local Wrangler CLI; unattended GitHub deployment remains pending that token.
- [YouTube catalog run 34146038610](https://github.com/liftaris/bazaar-ghost/actions/runs/34146038610) and [run 34146040402](https://github.com/liftaris/bazaar-ghost/actions/runs/34146040402) failed at the Python HTTP health preflight with HTTP 403 / Cloudflare 1010. Those runs did not reach provider cataloging or OCR. A browser-accessible health endpoint does not yet prove runner access.
- No hosted VOD has completed the catalog → GitHub OCR → D1/R2 path in this migration environment. Validation remains configured with outbound integrations disabled and no Cron schedules.
- Local follow-up changes are not part of deployed `2923d2a`: populated multiplatform snapshot export/import, additional scheduling/atomicity/search fixes, and the additive platform-contract migration. The snapshot tooling has passed local tests and a synthetic populated D1 import; it has not exported or imported hosted data.
- Review also identified a clean early FFmpeg end-of-file path which could mark an incompletely sampled chunk completed. Its correction remains in progress. Hosted acceptance must check expected sampled-frame coverage (for example, 450 frames for a 900-second range at 0.5 FPS), terminal states, persisted detections, and retrievable screenshots together.
- User authentication, sessions, likes, comments, and favorites are not implemented yet. An auth design has been reviewed and the Better Auth dependency has been added locally; this is preparatory work, not a working login or social API.

### 2026-09-07 — independent Worker contract review

- Reproduced and fixed three integration regressions with real local D1: ordinary YouTube appearances lost their populated `source_video_id`; a source disappearing between grouped-search reads could return an inconsistent group or throw; enabling a previously disabled historical catalog left its unplanned recordings invisible to the ingestion dispatcher. The source identity correction is additive migration `0003_platform_contracts.sql`; deployed migration `0002` remains unchanged.
- Account creation/enablement and durable account-poll scheduling now commit in one fenced transaction. Failure-injection tests lose the database response after commit and verify an enabled account still has its polling job. Automatic rediscovery continues to preserve operator disables.
- Both schedulers share bounded, due-work selection. Future-scheduled chunks do not crowd out ready recordings; default non-Twitch priority remains below Twitch and explicit chunk priorities take precedence. Reservation rechecks current enablement, availability, readiness, and schedule before publishing any GitHub work.
- Group selection and appearance aggregation use one SQL snapshot while preserving public response names and fields.
- Validation: `npm run typecheck`, `git diff --check`, and the complete local Workers/D1 suite passed: **86 tests across 7 files**, including **8 independent contract regression tests**. These are local runtime checks; hosted completion evidence is recorded separately by the deployment work.
- Additional review work identified that successful nonempty decoding did not prove coverage of the requested chunk. A separate media regression/fix is in progress; successful status alone must not be treated as end-to-end coverage evidence.

### 2026-09-07 — runner access and second platform revision

- Reproduced Cloudflare 1010 with urllib's generic default User-Agent. Added a truthful BazaarGhost application User-Agent to the no-redirect backend clients; the actual Python `/health` preflight now succeeds against validation before credentials are sent. Deployment health and dev storage clients share the corrected transport.
- Added reviewed-range input to the Bilibili workflow. This permits source-relative bounded smoke runs instead of cataloging an entire submission by accident.
- Reviewed Worker fixes pass 86 tests: one-snapshot appearance groups, source-video identity fallback, atomic account/poll scheduling, old unplanned catalog eligibility, due-time/priority selection, and disable-before-reservation checks.
- Updated populated snapshot support through platform migration 0003; auth/session migration remains explicitly excluded. The current script suite passes 76 tests and the targeted Docker persistence suite passes 5 tests.
- A real FFmpeg reproduction confirmed clean early EOF could incorrectly complete a partially decoded chunk. Coverage validation and regression tests are in progress; no hosted completion will be accepted from status alone.

### 2026-09-07 — decoded range coverage

- Reproduced a false completion with actual FFmpeg: a six-second MP4 requested as `[0,30)` returned exit code 0, emitted samples at 0/2/4 seconds, and the old runner marked the thirty-second chunk completed with only three frames.
- The decoder now verifies its first sample starts at the requested offset, successive sampled PTS values are continuous at the configured rate, and every sampling tick in the requested half-open range is present. Success also requires all decoded samples to pass through frame processing. Clean early EOF, a shifted start, missing samples, and duplicate timestamps fail the chunk instead of writing completed.
- Moved FFmpeg's duration limit to input reading because an output duration rounds to the encoder's coarse sampled time base. The documented `fps` filter `eof_action=pass` preserves a legitimate final partial sampling interval, allowing the expected count to be `ceil(duration * frame_rate)` rather than accepting a missing last tick. Sampling remains discrete; this verifies the sampled timeline, not exhaustive visual recall or the authenticity of upstream frames.
- Successful structured logs, returned results, and exported detection summaries include requested bounds, first/last sample timestamps, interval, expected minimum samples, and decoded sample count. Hosted audits can compare these with processed frames and persisted detections.
- Validation: **113 offline OCR tests passed in 69.71 seconds**, including **34 pipeline/media tests**. Real local MP4/HLS cases cover clean early EOF, a six-second source requested as seven seconds, nonzero and between-segment seeks, complete odd-length recordings, a one-second final range, and several sampling rates. `py_compile` and `git diff --check` also passed. Runtime source and test mounts included the current parent-owned backend/media client changes; the earlier cached Docker image alone does not contain this final source. Local evidence: `.ignore/cloudflare-platform-coverage-tests.log`.

FFmpeg reference: https://ffmpeg.org/ffmpeg-filters.html#fps and https://ffmpeg.org/ffmpeg.html (input duration option).

### 2026-09-07 — operator profile and creator-link controls

- Account PATCH/upsert now distinguishes an omitted `streamer_id` from explicit null. A reviewed clear removes the account link and existing recording links in one transaction; a catalog insert reads the current account link inside its own INSERT so an older metadata read cannot restore a cleared link. Twitch-owned records remain unchanged.
- Added narrow `PATCH /api/catalog/videos/<internal-id>` for manual profile set/clear, requiring the expected owning `account_id`. Automatic jobs cannot alter operator profiles or clear creator links. Routine cataloging preserves an existing video override.
- Effective profile changes are rejected atomically while affected work is queued/processing. No-ops and account changes that leave independently overridden videos unchanged are allowed. Dispatch reservation also verifies that the selected profile is still current before sending GitHub work.
- Added `scripts/catalog_settings.py` for these reviewed settings without upstream metadata requests; documented CLI/API semantics and sampled coverage evidence in `docs/multiplatform-backend.md`.
- Validation: all **9 new real-D1 operator tests** and **4 CLI tests** pass; strict TypeScript and diff checks pass. A full integration run at this point had **145 passing tests and one existing caching assertion mismatch** (`integrations.test.ts` expected max-age=30 while the concurrently updated visibility policy emitted max-age=0); that parent-owned integration is being aligned separately.

### 2026-09-07 — first real hosted processing result

- Deployed platform revision `93c82c6` (Worker version `15e5eabe-3850-4952-bd9a-97ab56ec032e`). GitHub catalog preflight now succeeds; Bilibili catalog run [34147086374](https://github.com/liftaris/bazaar-ghost/actions/runs/34147086374) persisted stable part `BV1FfL5zPEbH:29594289602` with old templates and reviewed interval `[600,1500)`.
- Published coverage/diagnostic revision `55a9deb`; [CI34147542161](https://github.com/liftaris/bazaar-ghost/actions/runs/34147542161) passed.
- [OCR34147607029](https://github.com/liftaris/bazaar-ghost/actions/runs/34147607029) completed that entire reviewed interval: 450/450 samples, first600s, last1498s, four D1 detections, four fetched JPEGs, and verified source-aware public search. This is a 900-second interval, not the whole7211-second recording. Evidence: `.ignore/platform-validation/bilibili-evidence.json` and the Actions artifact. Three names are visually clear; the narrow glyph in `Bluelay` versus `BlueJay` remains an explicit OCR-label uncertainty, despite agreement with historical output.
- YouTube diagnostic run [34147608712](https://github.com/liftaris/bazaar-ghost/actions/runs/34147608712) establishes that GitHub-hosted extraction requests human sign-in. The exact same public video resolves anonymously from the local OCR container. A temporary isolated self-hosted Actions runner is being prepared so cataloging/OCR execution still belongs to GitHub Actions without provider cookies or proxies.
- Local auth/social integration now includes session-based routing, CSRF/CORS, durable clips, likes, private favorites, comments, reports, moderation, expiry maintenance, and public visibility propagation. These remain undeployed until integrated verification and independent review finish. Real provider OAuth configuration/consent is pending user setup.

### 2026-09-07 — independent authentication contract review

- Reproduced raw OAuth provider `error_description` reflection in a successful HTTP 302 error redirect. The callback wrapper now normalizes its fixed error destination without query/fragment and discards the error redirect body; successful trusted application callback URLs remain intact.
- Found that Better Auth 1.7.3 unconditionally adds `__Secure-` when its secure-name option is enabled, turning the intended session cookie into `__Secure-__Host-bazaarghost_session`. That name lacked browser-enforced `__Host-` semantics, and logout cleared a different name. Auth now owns its cookie naming prefix while setting Secure attributes explicitly: HTTPS state/session cookies begin exactly `__Host-`, use HttpOnly/Secure/Path=/ without Domain, and logout expires the exact emitted session name. Local HTTP cookies remain unprefixed.
- Added independent tests for redirect/header privacy, preserved success URLs, exact state/session cookie names, logout expiry, local HTTP state, and racing first registrations. The parent integration tests also exercise login/session behavior through the full Worker wrapper.
- Final integrated validation: **165 Worker tests passed across 13 files in 24.78 seconds**, including the operator controls, auth regression tests, full Worker user API, and updated visibility/cache assertions. Strict TypeScript and diff checks pass; the earlier concurrent caching assertion mismatch is resolved. These mocked-provider/local-D1 checks do not replace real provider registration, browser consent, and hosted verification.
- Independently inspected the hosted Bilibili screenshots: `Pryss` (1034), `TheBigDipper` (1232), and `BELOVEDCOFFEE` (1424) are visually clear. At 822 the narrow glyph in `Bluelay` versus `BlueJay` remains uncertain; historical OCR agreement is not ground truth.

### 2026-09-07 — hosted session and social mechanics proof

- Ran the reviewed HTTP smoke against isolated validation Worker `1c5428c68ca035bab24b43fef1a34820435a8608`, with exact environment/build checks before each action. The successful run completed from `18:09:56Z` to `18:10:22Z`. It used two explicitly synthetic operator-created D1 users, Discord account fixtures, and fourteen-minute native ISO sessions, signed with the installed Better Call cookie helper. This validates hosted session/social mechanics; it does **not** validate real provider OAuth, browser consent, or a frontend login UI. No provider endpoint was called; outbound integrations stayed disabled.
- Verified safe authenticated identity/accounts/session responses, forged-cookie rejection, credentialed trusted-origin CORS, missing/cross-user CSRF rejection, unique two-user likes, private favorite/heart alias and idempotent removal, comment ownership/version conflicts/retry tombstones, session ownership and immediate revocation, exact cookie expiry, and account deletion cascades.
- Briefly hid existing Bilibili clip `1` through the admin moderation API. Previously requested public view/search/appearance URLs immediately excluded its detection, and screenshot GET/HEAD requests with and without ETag were denied. Restoring visibility returned the same 4,646 screenshot bytes with SHA-256 `03bf3eb0d204ec959993845f0ba2adf7c620a7f67ef75555890386bba2aaac38`. Its clip identity and original detection remained intact; all seventeen real clips/detections remained.
- Final D1 verification found zero synthetic users, accounts, sessions, likes, favorites, comments, reports, or social rate counters. All global auth row counts returned to zero, clip `1` returned to visible with zero likes, and token-containing seed SQL files were removed. Two admin moderation audit records remain as the actual operator trail; short-lived authentication rate counters expire through normal maintenance. Private evidence and CLI logs retain restrictive permissions.
- Evidence: `.ignore/platform-validation/hosted-social-smoke.6dfcb900-8bb2-4c85-8f5f-7c8e29409973/proof.json`; repeatable operator script: `.ignore/platform-validation/hosted-social-smoke.mjs`. An earlier harness attempt encountered Wrangler's non-JSON import notice despite `--json`; seed and finally-cleanup both succeeded, a separate remote query confirmed zero remaining users/accounts/sessions, and the parser was corrected before the successful run. No application defect was found by this smoke.
